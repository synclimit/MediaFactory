import { OutputAdapter } from './OutputAdapter';

/**
 * CanvasPreviewAdapter (Stub)
 * Reserved for direct HTML5 Canvas rendering.
 */
export class CanvasPreviewAdapter extends OutputAdapter {
    initialize() {}
    render(frame) {}
    dispose() {}
}
