import { OutputAdapter } from './OutputAdapter';

/**
 * ThumbnailAdapter (Stub)
 * Reserved for generating static thumbnails.
 */
export class ThumbnailAdapter extends OutputAdapter {
    initialize() {}
    render(frame) {}
    dispose() {}
}
