import { OutputAdapter } from './OutputAdapter';

/**
 * ScreenshotAdapter (Stub)
 * Reserved for single-frame captures from the timeline.
 */
export class ScreenshotAdapter extends OutputAdapter {
    initialize() {}
    render(frame) {}
    dispose() {}
}
