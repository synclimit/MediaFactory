/**
 * ExecutionStatus
 * Defines the final status of an EngineAdapter's execution within the RenderPipeline.
 */
export const ExecutionStatus = Object.freeze({
    SUCCESS: 'SUCCESS',
    SKIPPED: 'SKIPPED',
    FAILED: 'FAILED',
    FALLBACK: 'FALLBACK'
});
